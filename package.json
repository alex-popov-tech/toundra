{
  "name": "crunner",
  "version": "1.0.0",
  "dependencies": {
    "@types/node": "10.12.11",
    "@types/yargs": "12.0.1",
    "typescript": "3.2.1",
    "yargs": "12.0.5"
  },
  "scripts": {
    "clean": "rm -rf built",
    "compile": "tsc --project lib",
    "build": "npm run clean && npm run compile"
  },
  "bin": {
    "crunner": "built/bin/crunner.js"
  }
}
