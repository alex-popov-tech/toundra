import { Api } from './api';

const Test = Api.Test;
const test = Api.Test;
const Suite = Api.Suite;
const suite = Api.Suite;
const BeforeEach = Api.BeforeEach;
const beforeEach = Api.BeforeEach;
const BeforeAll = Api.BeforeAll;
const beforeAll = Api.BeforeAll;
const AfterEach = Api.AfterEach;
const afterEach = Api.AfterEach;
const AfterAll = Api.AfterAll;
const afterAll = Api.AfterAll;
const AddListener = Api.AddListener;
const addListener = Api.AddListener;

export {
   Test,
   test,
   Suite,
   suite,
   BeforeEach,
   beforeEach,
   BeforeAll,
   beforeAll,
   AfterEach,
   afterEach,
   AfterAll,
   afterAll,
   AddListener,
   addListener
};
